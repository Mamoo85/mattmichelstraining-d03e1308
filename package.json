{
  "name": "m2training-lovable-preview",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "npm --prefix frontend install --include=dev",
    "dev": "npm --prefix frontend run dev --",
    "prebuild": "npm --prefix frontend ci --include=dev || npm --prefix frontend install --include=dev",
    "build": "npm --prefix frontend run build && rm -rf dist && cp -R frontend/dist dist",
    "build:dev": "npm --prefix frontend run build:dev",
    "preview": "npm --prefix frontend run preview"
  }
}