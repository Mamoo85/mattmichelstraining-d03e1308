import { Button } from "@/components/ui/button";

const ManageBillingButton = ({ className }: { className?: string }) => (
  <Button variant="outline" className={className}>Manage Billing</Button>
);
export default ManageBillingButton;
